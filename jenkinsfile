pipeline {
    agent any
    stages {
        stage('Repo checkout'){
            steps{
                checkout scm
            }
        }
        stage('NPM Install') {
            steps {
                sh "npm install"
            }
        }
        stage('Run integration tests') {
            steps {
                sh "npm run test"
            }
        }
    }
}